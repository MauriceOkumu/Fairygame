{
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd frontend && yarn && yarn run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "path": "^0.12.7"
  },
  "proxy": "http://localhost:5000"
}
